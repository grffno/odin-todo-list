class Todo {
  constructor(id, title, notes, dueDate, priority) {
    this.id = id;
    this.title = title;
    this.notes = notes;
    this.dueDate = dueDate;
    this.priority = priority;
  }
}

export default Todo;
